{
  "name": "@mikconnect/desktop",
  "version": "1.0.0",
  "private": true,
  "description": "MikConnect Flash - Desktop app for hotspot management",
  "main": "main.js",
  "scripts": {
    "dev": "electron .",
    "build": "electron-builder build --win --config build.config.json",
    "dist": "electron-builder --win --x64"
  },
  "dependencies": {
    "electron-store": "^8.1.0"
  },
  "devDependencies": {
    "electron": "^28.1.0",
    "electron-builder": "^24.9.1"
  }
}
