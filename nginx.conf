# Frontend + API proxy
# For SSL, deploy behind Cloudflare or use certbot with the commented-out SSL block below
server {
    listen 80;
    server_name mikconnect.com www.mikconnect.com;

    root /usr/share/nginx/html;
    index index.html;

    gzip on;
    gzip_types text/css application/javascript image/svg+xml;
    gzip_min_length 256;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        proxy_pass http://backend:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}

# Uncomment for SSL with certbot:
# server {
#     listen 443 ssl;
#     server_name mikconnect.com www.mikconnect.com;
#     ssl_certificate /etc/letsencrypt/live/mikconnect.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/mikconnect.com/privkey.pem;
#     ...
# }
